description: "Multiple agent violations are all surfaced via errors.Join (not fail-fast)"
input: |
  name: multi-agent-errors
  agents:
    coder: {}
    reviewer: {}
  steps:
    - id: work
      agent: coder
      instructions: "Do some work"
expected:
  valid: false
  error: agent_missing_description
