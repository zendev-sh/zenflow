node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/

# OS / editor noise.
**/.DS_Store
*.swp
*.swo
.idea/
.vscode/

# CLI binary built from ./cmd/zenflow (root output or in-package `go build`).
/zenflow
/cmd/zenflow/zenflow

# Go coverage profiles (`go test -coverprofile=...`, e.g. cov.out).
*.out

# Maintainer-only git hooks (never committed; mirrors goai's convention).
hooks/
cover.out

# Generated docs-drift audit reports.
docs/drift-report/
